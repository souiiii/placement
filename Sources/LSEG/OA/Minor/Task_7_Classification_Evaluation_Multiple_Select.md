# Task 7 - Classification Evaluation Multiple-Select

**Classification:** Minor

## Question

The exact original question and option text are not preserved in the supplied PDF.

The preserved answer shows that this was a multiple-select question involving model evaluation under class imbalance and the cost of false positives.

## Answer

Select:

```text
A, B, C, E, G, H
```

Do not select:

```text
D, F
```

Preserved rationale:

- `D` should not be selected because maximizing recall alone may generate too many costly false positives.
- `F` should not be selected because class imbalance can make accuracy highly misleading.
